import React from 'react';

const Reported = () => {
    return (
        <div>
            this is report
            <label
                htmlFor="adminDrawer"
                className="btn btn-primary drawer-button lg:hidden"
              >
                Open drawer
              </label>
        </div>
    );
};

export default Reported;